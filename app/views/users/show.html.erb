<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<div class="span8">
    <% if @user.pizzas.any? %>
      <h3>Pizzas(<%= @user.pizzas.count %>)</h3>
      <ol class="pizzas">
        <%= render @pizzas %>
      </ol>
    <% end %>
</div>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
